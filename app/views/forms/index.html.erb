<div class="form_page">
<h1><%= @user.name %></h1>
<img src="/assets/pasaporte.jpg" alt="Passport" class="pasaporte">

<ul>
	<% @forms.each do |form| %>
		<li>
			<%= form.answer%> 
			<%= form.answerb%> 
			<%= form.answerc %>
			<%= form.answerd %>
			<%= link_to('Edit', edit_user_form_path(@user, form)) %>
			<%= link_to('Delete', user_form_path(@user, form), method: :delete, data: {confirm: 'Are you sure?'}) %>
		</li>
	<% end %>
</ul>
<div>
<%= link_to('Fiancé Form', new_user_form_path(@user)) %>
<br>
<%= link_to('Back', users_user_form_path) %>
 <%= link_to "Logout", destroy_user_session_path, data: { method: :delete } %>
</div>
 </div>  
